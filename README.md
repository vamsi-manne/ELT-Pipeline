ELT pipeline with incremental loading and transformation data by following Medallion Architecture

Transforming the data using Slowly Changing Dimensions into one transformation pipeline

SCD Type1 --> which holds the updated data without maintaining historical data
SCD Type2 --> which maintains the historical data along with old data
